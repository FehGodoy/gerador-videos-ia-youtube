# Python
.venv/
__pycache__/
*.pyc

# Env / secrets
.env

# Node / Remotion
remotion/node_modules/
remotion/out/

# Gerado pelo pipeline (cache local e outputs, não versionar)
cache/*
!cache/.gitkeep
output/*
!output/.gitkeep

# Sistema
.DS_Store
Thumbs.db

# Estado local do Claude Code (aprovações de permissão, locks) — não é config compartilhada
.claude/
