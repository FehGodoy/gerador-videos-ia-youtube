requests>=2.32
websocket-client>=1.8
pydub>=0.25
audioop-lts>=0.2 # backport do módulo audioop, removido do stdlib no Python 3.13+ (pydub depende dele)
PyYAML>=6.0
python-dotenv>=1.0
anthropic>=0.40
jsonschema>=4.23
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12 # upload de arquivo (UploadFile) no painel web — sem isso o FastAPI recusa multipart/form-data
# Download de imagem do Google Imagens abrindo a página de origem num
# Chromium real (com os cookies do usuário) em vez de pedir a URL do arquivo
# direto — contorna proteção anti-hotlink que barra requisição de script.
# Depois de instalar, rodar: python -m playwright install chromium
playwright>=1.62

# Fallback de transcrição, usado apenas se a Cartesia não retornar timestamps
# para algum beat (ver modules/captions.py). Não é chamado no fluxo padrão.
faster-whisper>=1.0

# Revalidação do arquivo baixado (não só a miniatura julgada pelo ranking) —
# ver _validate_downloaded_media em modules/footage_search.py.
Pillow>=10.0        # dimensão real de imagem baixada
imagehash>=4.3       # confere se o arquivo final é o mesmo da miniatura (google_images/youtube)
